## devicemanager config

Manage CLI configuration

### Examples

```
  devicemanager config current-context
  devicemanager config list-contexts
  devicemanager config use-context my-context
  devicemanager config delete-context old-context
```

### Options

This command has no specific options. See subcommands.
