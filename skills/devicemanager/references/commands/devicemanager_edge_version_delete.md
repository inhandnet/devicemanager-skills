## devicemanager edge version delete

Delete an edge app version

### Examples

```
  devicemanager edge version delete <app-id> <version>
```

### Options

```
  <app-id>   App ID (required positional argument)
  <version>  Version string (required positional argument)
  -y, --yes  Skip confirmation prompt
```
