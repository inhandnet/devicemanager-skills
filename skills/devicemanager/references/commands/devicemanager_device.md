## devicemanager device

Manage devices

### Examples

```
  devicemanager device list
  devicemanager device get <device-id>
  devicemanager device create --name "test" --serial-number SN001
```

### Options

This command has no specific options. See subcommands.
