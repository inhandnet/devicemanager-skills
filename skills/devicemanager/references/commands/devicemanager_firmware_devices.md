## devicemanager firmware devices

Manage devices in a firmware upgrade job

### Examples

```
  devicemanager firmware devices list <firmware-id>
  devicemanager firmware devices add <firmware-id> <device-id>
  devicemanager firmware devices remove <firmware-id> <device-id>
```

### Options

This command has no specific options. See subcommands.
