## devicemanager device config

Manage device configuration

### Examples

```
  devicemanager device config get <device-id>
  devicemanager device config set <device-id> --content "..."
```

### Options

This command has no specific options. See subcommands.
