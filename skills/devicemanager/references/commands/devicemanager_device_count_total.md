## devicemanager device count total

Query total device count over time

```
devicemanager device count total [flags]
```

### Options

```
      --end-time string     End date (YYYY-MM-DD, e.g. 2026-05-01)
  -h, --help                help for total
      --start-time string   Start date (YYYY-MM-DD, e.g. 2026-04-01)
```

### Options inherited from parent commands

```
      --context string   Override active context (env: DEVICEMANAGER_CONTEXT)
      --debug            Enable debug output (env: DEVICEMANAGER_DEBUG)
      --jq string        Filter JSON output using a jq expression (implies -o json)
  -o, --output string    Output format: json, table, yaml (default: table for TTY, json otherwise)
```
