## devicemanager device reboot

Reboot a device

### Examples

```
  devicemanager device reboot <device-id>
  devicemanager device reboot <device-id> --timeout 30000
```

### Options

```
      --timeout int  Timeout in milliseconds (default 15000)
```
